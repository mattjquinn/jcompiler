program = _{ SOI ~ (stmt ~ "\n") * ~ stmt? ~ EOI }

stmt = _{ expr }

expr = _{ exprBinAdd | exprNum }
exprBinAdd = { number ~ "+" ~ number }
exprNum = { number }

number = { ASCII_DIGIT+ }

WHITESPACE = _{ " " | "\t" }
