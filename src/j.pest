program = _{ SOI ~ (stmt ~ "\n") * ~ stmt? ~ EOI }

stmt = _{ expr }

expr = { term ~ (binOp ~ term)* }
term = _{ number | "(" ~ expr ~ ")" }

binOp = { "+" | "*" }
number = { ASCII_DIGIT+ }

WHITESPACE = _{ " " | "\t" }
