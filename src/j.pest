program = _{ SOI ~ (stmt ~ "\n") * ~ stmt? ~ EOI }

stmt = _{ expr }

expr = _{   exprBinOp
          | exprNum }
exprBinOp = { number ~ binOp ~ number }
exprNum = { number }

binOp = { "+" | "*" }
number = { ASCII_DIGIT+ }

WHITESPACE = _{ " " | "\t" }
